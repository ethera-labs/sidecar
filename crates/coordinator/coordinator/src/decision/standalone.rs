//! Standalone decision logic.
//!
//! In standalone mode, decisions are computed inline by
//! `DefaultCoordinator::maybe_make_standalone_decision` in `coordinator.rs`.
//! No separate handler type is needed.
